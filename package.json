{
  "name": "2fa-live",
  "version": "0.1.0",
  "description": "Open-source browser extension that shows live TOTP 2FA codes from a user-provided secret.",
  "type": "module",
  "author": "Trang Ha Viet",
  "license": "MIT",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "pack:extension": "node scripts/pack-extension.mjs",
    "preview": "vite preview --host 127.0.0.1",
    "check": "svelte-check --tsconfig ./tsconfig.json"
  },
  "dependencies": {
    "@crxjs/vite-plugin": "^2.6.1",
    "@sveltejs/vite-plugin-svelte": "^7.1.2",
    "svelte": "^5.0.0",
    "vite": "^8.0.16"
  },
  "devDependencies": {
    "@tsconfig/svelte": "^5.0.4",
    "svelte-check": "^4.0.0",
    "typescript": "^5.6.0"
  }
}
